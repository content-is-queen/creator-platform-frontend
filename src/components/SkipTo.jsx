const SkipTo = () => (
  <a
    className="sr-only focus:not-sr-only block text-queen-white focus-visible:no-outline bg-queen-orange"
    href="#main-content"
  >
    Skip to main content
  </a>
);

export default SkipTo;
