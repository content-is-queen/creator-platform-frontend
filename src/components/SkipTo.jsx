const SkipTo = () => (
  <a
    className="text-center sr-only focus:not-sr-only block text-queen-white focus-visible:outline-none bg-queen-orange"
    href="#main-content"
  >
    Skip to main content
  </a>
);

export default SkipTo;
